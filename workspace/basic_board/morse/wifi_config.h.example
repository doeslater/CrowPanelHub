// Copy this file to wifi_config.h and fill in your real credentials.
// wifi_config.h is the one the sketch actually includes — keep it out of
// version control since it holds your WiFi password in plain text.

#ifndef WIFI_CONFIG_H
#define WIFI_CONFIG_H

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

#endif
